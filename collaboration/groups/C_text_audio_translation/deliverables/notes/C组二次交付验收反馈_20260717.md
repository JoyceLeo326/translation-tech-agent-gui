# C组二次交付验收反馈

日期：2026-07-17

## 这次已经补上的内容

1. 文字/DOCX 工作流补充了 5 组可复测样例，包括原始 DOCX、审核 Excel 和最终英文 DOCX。
2. 文字/DOCX 工作流补充了项目 ID、工作日志、截图和链接材料。
3. 音视频工作流补充了测试音频、测试表格、模式二输出总音频、四列输出表格、二维码和截图。
4. 音视频工作流补充了 Canvas ID、部署域名、异步接口说明和验收文档。

## 我已经在整合侧处理的内容

1. 文字/DOCX 源码中删除了缺少 `file_docx` 时回退到样例 Coze URL 的逻辑。
2. 文字/DOCX 支路二改为必须同时提供 `review_excel` 和原始 `file_docx`。
3. 文字/DOCX 支路二新增 `docx_replace_report` 输出，用于检查替换覆盖、未命中项和中文残留。
4. 文字/DOCX Excel 列识别改为显式别名匹配，关键列缺失时直接报错。
5. 文字/DOCX LLM 调用补回 `thinking` 配置兼容处理。
6. 仓库目录、二次交付材料、截图、日志和补充样例已完成整理入库。

## 还需要 C 组必须补充或确认

1. **必须更新扣子平台上的文字/DOCX 工作流画布**：当前二次提交的源码包里仍带有样例 URL 兜底逻辑，虽然我已经在 GitHub 版源码修掉，但 C 组需要确认平台画布也已同步修正。要求：支路二缺少 `file_docx` 时直接报错，不允许使用任何固定样例 DOCX URL。
2. **必须确认平台输出变量包含 `docx_replace_report`**：GitHub 源码已加，但如果 Coze 画布输出变量没有同步，前端/总整合拿不到替换覆盖报告。
3. **必须把文字/DOCX 工作流文档和平台入参说明改成一致**：支路二 `file_docx` 不是“可选/推荐”，而是必填。
4. **音视频工作流如走线上 API，对接 Token 必须通过安全渠道单独给后端负责人**：不要放进 GitHub、Word 文档或截图里。
5. **音视频工作流需确认最终对接方式**：模式二必须支持显式传入 `finalized_excel + original_audio_url`，不能只依赖 Supabase 最近一次缓存；否则总 App 在跨会话、多人测试或重新打开应用时会不稳定。
6. **正式验收如需要追溯，请补模式一/模式二的 Coze run_id 或平台导出的运行日志 JSON**：目前补充材料有结果文件，但没有完整运行日志链路。

## 当前结论

本次 C 组二次交付比上一版完整很多，文字和音视频两条通道都可以进入整合仓库。文字/DOCX 的平台侧画布同步是当前最关键风险；音视频主要剩余风险是线上 API Token 安全交付、跨会话原始音频复用策略和运行日志可追溯性。
