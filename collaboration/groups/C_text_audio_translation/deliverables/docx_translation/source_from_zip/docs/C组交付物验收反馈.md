# C组交付物最终反馈

检查对象：`workflow_c_docx_translation_20260715`

检查口径：我负责最后处理和整合。凡是我能直接改代码、补文档、清包、打包解决的，不要求 C 组返工。下面只列必须由 C 组补充、确认或重新提供的内容。

## 结论

C 组当前源码可以进入后续整合，我这边可以继续处理代码清理、打包、文档标准化、部分健壮性修复。

但 C 组还必须补齐真实交付证据和业务确认。没有这些内容，我无法最终证明这个仓库交给别人后一定能跑通，也无法确认生成结果是否符合他们原始任务目标。

## 必须让 C 组补充或确认的内容

### 1. 提供一整套真实可复测材料

现在包里没有实际测试素材。C 组需要提供他们测试过的完整链路材料，而不是只给文字描述。

请 C 组补：

```text
1. 原始中文 DOCX
2. 支路一生成的三列对照 Excel
3. 人工审核后的 Excel
4. 支路二生成的英文 DOCX
5. 对应的运行日志或测试说明
```

这些文件用于最终验收：

```text
DOCX → Excel 是否逐行对齐；
人工审核列是否按预期回填；
Excel → 英文 DOCX 是否替换完整；
英文 DOCX 是否还有中文残留；
格式保留是否符合他们的预期。
```

这是必须让 C 组给的，不能由我凭空造一套样例替代。

### 2. 确认支路二的真实使用方式

当前代码和文档都表达了“上传审核 Excel 后生成英文 DOCX”，但支路二到底怎么拿到原始 DOCX，需要 C 组明确。

请 C 组确认二选一：

```text
A. 支路二要求用户同时上传：审核 Excel + 原始 DOCX；
B. 支路二不让用户重复上传原始 DOCX，而是自动复用支路一上传过的 DOCX。
```

如果选 A，我会把流程整理成强制双文件输入。

如果选 B，C 组必须提供他们原本设计的复用机制说明，例如：

```text
原始 DOCX 存在哪里；
用什么字段/session_id 关联；
缓存有效期多久；
支路二怎么取回原始 DOCX。
```

这个属于业务链路确认，不能由我猜。

### 3. 提供扣子/Coze 侧真实工作流信息

按之前搭仓库的交付方式，最终不是只给源码，还要能让同学自己部署或复现工作流。C 组需要提供他们在扣子/Coze 侧的实际项目信息。

请 C 组补：

```text
1. 扣子/Coze 工作流链接，或项目/工作流 ID；
2. 输入变量截图或说明；
3. 输出变量截图或说明；
4. 用到的模型、对象存储、文件上传等平台能力截图或配置说明；
5. 如果无法分享链接，至少给可导入/可复建的配置说明。
```

我可以整理仓库，但不能替他们证明扣子平台上的工作流配置确实存在、变量确实一致。

### 4. 提供外部依赖资源的确认清单，不要给密钥

不需要 C 组提供任何真实密钥。但他们需要确认这套工作流运行时依赖哪些外部资源，以及每个同学自行部署时需要自己开通什么。

请 C 组确认：

```text
1. LLM 模型名称和可用权限；
2. 对象存储/S3/TOS 是否必须；
3. 文件上传和预签名 URL 的有效期要求；
4. 是否依赖数据库或会话缓存；
5. 部署时需要配置的环境变量名称。
```

这部分如果缺失，别人拿仓库部署时会卡在资源配置，不是代码本身能完全解决的问题。

### 5. 提供他们认可的验收标准

C 组需要明确最终怎么判定“生成成功”。现在只看到“0 中文残留、格式保留”等描述，但没有验收口径。

请 C 组给出最小验收标准：

```text
1. Excel 行数是否必须等于解析句子数；
2. 中文残留允许范围：完全 0，还是标题/页眉等特殊区域可例外；
3. 格式保留要求：只保留段落/run 样式，还是表格、页眉页脚、文本框都必须保留；
4. 文件名翻译是否必须由 LLM 生成，还是可按固定规则命名；
5. 支路二失败时应该中断，还是允许部分替换输出。
```

这决定我后续整合时应该按什么标准修代码和做最终检查。

## 不需要 C 组返工、我这边可直接处理的内容

以下内容不再作为 C 组反馈要求：

```text
1. 清理 .venv/.codegraph/__pycache__；
2. 重新打 ZIP；
3. 标准化交付日志；
4. README/SETUP/API 文档整理；
5. 删除样例 URL fallback；
6. 调整 Excel 列识别逻辑；
7. 对 LLM 调用参数和文档表述做一致性修正；
8. 增加本地检查脚本或中文残留扫描脚本。
```

这些我可以在最终整合阶段直接处理，不需要 C 组来回返工。

## 可以直接发给 C 组的话

```text
C 组这边源码我们可以继续整合，不需要你们返工文档和打包细节。

现在必须请你们补 5 类信息：

1. 给一整套真实测试材料：原始 DOCX、支路一生成 Excel、人工审核 Excel、最终英文 DOCX、运行日志/测试说明。
2. 确认支路二到底是“审核 Excel + 原始 DOCX 同时上传”，还是“自动复用支路一原始 DOCX”。如果是自动复用，请说明存储和关联机制。
3. 提供扣子/Coze 侧工作流链接或项目 ID，并给输入输出变量、模型、对象存储等平台配置说明。
4. 确认外部依赖资源清单：模型权限、对象存储、URL 有效期、是否需要数据库/缓存、环境变量名称。不需要提供密钥。
5. 给出你们认可的验收标准：中文残留、格式保留范围、行数对齐、文件命名、失败时是否允许部分输出。

这些是最终仓库交付和验收必须用到的材料，不是文档格式问题。
```
