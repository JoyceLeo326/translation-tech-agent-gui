# B 组交付物验收反馈

检查日期：2026-07-15
检查口径：凡是可以由整合侧完成的归档、抽取、格式转换、共享目录同步，不要求 B 组返工；只把必须由 B 组确认、补充或重新导出的内容列出。

## 已收到并归档的材料

1. `B组工作流prd文档.docx`：已归档到 `deliverables/prd/`，并抽取为 Markdown。
2. `Workflow-Translation-draft-9476.zip`：已归档并展开，包含 `MANIFEST.yml` 和 `workflow/Translation-draft.yaml`。
3. `中华文化术语对照表.xlsx`：已归档到 `terminology/`，并同步到 `collaboration/shared/terminology/`。
4. `翻译资源编写-中国文化知识百科（图文有声版）.pdf`：已归档到 `references/`，并抽取文本用于核验。
5. 聊天截图 3 张：已归档到 `deliverables/notes/chat_screenshots/`。

## 完整度检查结论

B 组材料整体比 A/C 组更完整，已经具备“术语库 + 来源 PDF + PRD + Coze 工作流导出包 + 工作流截图/聊天说明”的主干。

术语库核验结果较好：Excel 共 212 条术语，5 个字段均无缺失，0 个重复术语；PDF 共 13 页，212 个中文术语均能在 PDF 抽取文本中找到。整合侧已生成 `zhonghua_culture_terms.normalized.csv` 和 `zhonghua_culture_terms.normalized.json`，可直接供程序检索。

工作流导出包可解析：YAML 中有 18 个节点、28 条边，包含风格提炼、术语提取、三模型初译、学习评估、总控融合、辩论评审和最终输出。

## 必须反馈 B 组补充或确认

### 1. 提供一次真实运行样例和结果

当前材料没有完整运行样例，无法验证工作流是否真实跑通、输出质量是否稳定。请 B 组补充一组最小但完整的测试记录：

1. 输入 `input_text` 和 `input_title`。
2. 工作流最终输出 `final_output`。
3. Coze 运行成功截图或运行日志截图。
4. 至少保留关键中间结果：`style`、`term`、`final_translation`、`debate` 或三模型辩论结果之一。

没有这组材料时，整合侧只能证明工作流配置文件存在，不能证明工作流真实可运行。

### 2. 提供 Coze 侧可复现信息

压缩包里有工作流 ID `7661678571702747178`，术语知识库 ID `7661642922585440283`，但没有可访问链接、发布状态或导入说明。请 B 组补充：

1. Coze 工作流链接或项目/工作流 ID 的截图。
2. 是否已发布，还是仅为草稿导出。
3. 中国文化术语知识库的绑定方式、知识库名称、是否需要单独导入 PDF/Excel。
4. Kimi 文献搜索工具、联网问答、豆包、DeepSeek、GLM 模型是否需要特殊权限或额度。

不需要提供任何账号密码或密钥。

### 3. 统一 PRD 与实际 YAML 的重试配置

PRD 第 9 节写“失败重试：1 次”，但实际 `Translation-draft.yaml` 中可见的 16 个可执行节点均为 `retryTimes: 0`。请 B 组二选一处理：

1. 如果实际设计是不重试，请更新 PRD，把“失败重试”改成 0 次。
2. 如果实际设计是失败重试 1 次，请在 Coze 工作流里重新配置并导出新版 zip。

### 4. 确认代码节点入参写法

PRD 第 10 节写“Coze 代码节点输入需通过 `args['params']` 获取”。实际 YAML 中：

1. `风格术语聚合` 使用 `args.get('style')` 和 `args.get('term')`。
2. `初译聚合` 使用 `args.get('dou1output')` 等。
3. `辩论聚合` 使用 `args.get('params', {})`。

请 B 组用真实运行记录确认这三类写法在当前 Coze 版本中都能拿到入参；如果不能，请统一改成平台实际支持的写法并重新导出。

### 5. 修正原始术语表中的英文弯引号

原始 Excel 中第 28、38、105 行英文译名包含弯引号：

| 行号 | 术语 | 原英文 |
| --- | --- | --- |
| 28 | 匠心 | `Craftsman’s Ingenuity` |
| 38 | 明心见性 | `Illuminating the Mind and Seeing One’s Nature` |
| 105 | 拙政园 | `Humble Administrator’s Garden` |

整合侧生成的 CSV/JSON 已自动规范为直引号，但建议 B 组同步修正源 XLSX，避免后续检索或字符串比对出现不一致。

## 不需要 B 组返工、整合侧已处理

1. 原始材料已按 B 组目录归档。
2. Coze zip 已展开，工作流摘要已生成。
3. PRD 已抽取为 Markdown。
4. PDF 已抽取文本，用于术语库来源核验。
5. 术语库已同步到共享目录。
6. 术语库已生成 CSV/JSON 机器可读版本。
7. 原始文件 SHA-256 校验清单已生成。

## 可直接转发给 B 组的短版

B 组这边材料主干完整，术语库、来源 PDF、PRD 和 Coze 工作流导出包都已经归档进 GitHub。需要你们补充 4 件事：第一，给一组真实运行样例，包括输入、最终输出、成功截图和关键中间结果；第二，给 Coze 工作流链接/项目 ID、发布状态、知识库绑定方式和模型/插件权限说明；第三，确认 PRD 写的“失败重试 1 次”和实际 YAML 的 `retryTimes: 0` 哪个才是准的；第四，确认 3 个代码节点的入参写法都在 Coze 当前版本实测可用。另请把术语表第 28、38、105 行英文里的弯引号改成直引号，方便后续程序检索。
