# 译述 Web 展示页

这是 Joyce 开发的桌面版 `译述 YISHU` 的公开练习、展示与下载入口，以 GitHub Pages 为推荐入口、Vercel 为备用入口。访客可在浏览器中比较三版译文、确认、下载审校记录，并让反馈重排下一轮候选；这些内容是明确标注的本机规则样本，不上传文件或调用在线模型。处理真实文件的完整能力位于 Windows 桌面版。

本地预览：

```powershell
python -m http.server 4173 --directory web
```
